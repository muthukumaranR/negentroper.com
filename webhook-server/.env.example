# GitHub Webhook Server Configuration

# Server configuration
PORT=3001
NODE_ENV=production

# GitHub webhook secret (generate a secure random string)
# This should match the secret configured in your GitHub webhook settings
GITHUB_WEBHOOK_SECRET=your_webhook_secret_here

# Repository and content paths
REPO_PATH=/path/to/your/repository
CONTENT_DIR=./content
PUBLIC_DIR=./public

# Auto-restart configuration
AUTO_RESTART=true

# Security configuration
ALLOWED_ORIGINS=http://localhost:3000,https://negentroper.com,https://www.negentroper.com

# Logging level (error, warn, info, debug)
LOG_LEVEL=info

# Optional: Database connection (if needed for future features)
# DATABASE_URL=postgresql://user:password@localhost:5432/negentroper

# Optional: Additional security headers
FORCE_HTTPS=true
TRUST_PROXY=true